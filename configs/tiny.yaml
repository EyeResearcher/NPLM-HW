seed: 1337
tokenizer_type: word
tokenizer_path: artifacts/wikitext2_word_vocab.json
context_size: 5
embedding_dim: 32
hidden_dim: 64
activation: tanh
dropout: 0.0
optimizer: adam
lr: 0.001
batch_size: 128
max_steps: 3000
log_every: 100
eval_every: 100
clip_grad_norm: 1.0
device: cpu
num_workers: 0
run_name: null
